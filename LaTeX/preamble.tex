\documentclass[a4paper,14pt,russian,oneside,final]{extreport}
\usepackage[T2A]{fontenc}
\usepackage[cp1251]{inputenc}
\usepackage{cmap}
\usepackage[warn]{mathtext}
\usepackage[russian,english]{babel}
\usepackage{extsizes,amsmath,amsfonts,amssymb}
\usepackage{amsthm,calc,fp,cite}
\usepackage[left=2.5cm,top=2.0cm,right=1.5cm,bottom=2.0cm]{geometry}
%\usepackage[nodisplayskipstretch]{setspace}
%\setstretch{1.3}
%\linespread{2}

\usepackage{listings}
\usepackage{setspace}

\mag 1440 \sloppy\makeatletter
% Выбор шрифта по-умолчанию.
 \usepackage{pscyr}
 \renewcommand{\rmdefault}{ftm} % Times New Roman

\usepackage{indentfirst} % Красная строка после заголовка
\setlength\parindent{1.25cm}


% Используем картинки 
\usepackage{graphicx}
\graphicspath{ {images/} }
% Переименовываем "Figure N" в подписях картинок в "Рис. N"
\usepackage{caption}
\captionsetup[figure]{name={Рис.},labelsep=period}
%\addto\captionsenglish{\renewcommand{\figurename}{Рис.}}

%этот пакет - для того, чтобы можно было принудительно размещать картинки в месте объявления 
%(т.е. аргумент [H] в \begin{figure}
\usepackage{float}

% Отключает использование изменяемых межсловных пробелов
\frenchspacing

% Переопределяем стандартную нумерацию, т.к. в отчете будут только section и т.д. в терминологии TeX
\makeatletter
\renewcommand{\thesection}{\arabic{section}}
\makeatother

% Работа с колонтитулами
\usepackage{fancyhdr} % пакет для установки колонтитулов
\pagestyle{fancy} % смена стиля оформления страниц

% Нумерация страниц располагается справа снизу страницы
\fancyhf{} % очистка текущих значений
\fancyfoot[R]{\thepage} % установка верхнего колонтитула
\renewcommand{\footrulewidth}{0pt} % убрать разделительную линию внизу страницы
\renewcommand{\headrulewidth}{0pt} % убрать разделительную линию вверху страницы
%\fancypagestyle{plain}{
%    \fancyhf{}
%    \rfoot{\thepage}}
\fancyhf{}
\fancyhead[C]{\thepage}
\pagestyle{fancy}

% Задает стиль заголовков подразделов
\makeatletter
\renewcommand\subsection{%
  \@startsection{subsection}{2}%
    {\fivecharsapprox}%
    {-1em \@plus -1ex \@minus -.2ex}%
    {1em \@plus .2ex}%
    {\raggedright\hyphenpenalty=10000\normalfont\normalsize\bfseries}}
\makeatother

% Задает стиль заголовков пунктов
\makeatletter
\renewcommand\subsubsection{
  \@startsection{subsubsection}{3}%
    {\fivecharsapprox}%
    {-1em \@plus -1ex \@minus -.2ex}%
    {\z@}%
    {\raggedright\hyphenpenalty=10000\normalfont\normalsize\bfseries}}
\makeatother

% Это я тут нахимичил
\makeatletter
\renewcommand\section{%
	\@startsection {section}{1}%
	{\z@}%
	{-1em \@plus -1ex \@minus -.2ex}%
	{1em \@plus .2ex}%
	{\hyphenpenalty=10000\normalfont\large\bfseries}%
}
\makeatother

% Для оформления введения и заключения (выравнивание по центру)
\makeatletter
\newcommand\sectioncentered{%
	\clearpage\@startsection {section}{1}%
    {\z@}%
    {-1em \@plus -1ex \@minus -.2ex}%
    {1em \@plus .2ex}%
    {\centering\hyphenpenalty=10000\normalfont\large\bfseries\MakeUppercase}%
    }
\makeatother

% Задает стиль библиографии
\bibliographystyle{styles/belarus-specific-utf8gost780u}
%\addto\captionsrussian{\renewcommand\bibname{
%		\large\bfseries\uppercase{
%			\centerline{Список используемых источников}}
%	% следующая строка - грязный хак, не надо её трогать, а то не будет компилироваться.
%	\global\def\bibname{Список используемых источников} % да, вот эта.
%}}
\makeatletter
\def\@biblabel#1{#1. } % меняем вид нумерации
\makeatother

\newtheorem{Th}{Теорема}
\newtheorem{Def}{Определение}
\newtheorem{Example}{Пример}[section]
\newenvironment{ProofRus}
{\par\noindent{\bf Замечание.}}
{\hfill}
\newcounter{abcd}